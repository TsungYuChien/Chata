<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="yzujbs.chata">

    <application
        android:allowBackup="true"
        android:icon="@drawable/chata_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/chata_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ListTalent"
            android:label="@string/title_activity_list_talent"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".BottomNavigation" />
        <activity android:name=".FillresumePage" />
        <activity android:name=".RegisterPage" />
        <activity android:name=".LoginPage" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>